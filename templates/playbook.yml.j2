- name: some playbook name
  hosts: localhost  # assuming podman runs on the local host
  gather_facts: false
  become: true  # we don't expect most docker compose to work without root

  tasks:
{% for task in tasks %}
    - {{ task }}
{%- endfor %}
